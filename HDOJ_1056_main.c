#include <stdio.h>
#include <stdlib.h>
int a[550];
void F()
{
    for(int i=1;i<=520;i++)
    {
        a[i]=
    }
}
int main()
{
    double n;
    while(scanf("%f",&n)&&n!=0)
    {
        printf("%d card(s)\n",F(n));
    }
    return 0;
}
